{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build-ActiveProperyCard": "cd nova-components/ActiveProperyCard && npm run dev",
        "build-ActiveProperyCard-prod": "cd nova-components/ActiveProperyCard && npm run prod",
        "build-activeproperycard": "cd nova-components/Activeproperycard && npm run dev",
        "build-activeproperycard-prod": "cd nova-components/Activeproperycard && npm run prod"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.5",
        "vite": "^4.0.0"
    }
}